description: GPIO pin to switch a power regulator on or off

compatible: "power-switch"

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      The GPIO connected to the enable pin of a voltage regulator.